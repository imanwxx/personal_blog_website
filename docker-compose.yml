version: '3.8'

services:
  personal_blog:
    build: .
    container_name: personal_blog
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./public/uploads:/app/public/uploads
    restart: unless-stopped
    environment:
      - NODE_ENV=production